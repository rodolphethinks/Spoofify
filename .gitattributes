# Force LF line endings for files that run inside the Linux Docker container,
# regardless of core.autocrlf on developer machines (Windows converts to CRLF
# by default, which breaks shell scripts' shebang/execution).
*.sh text eol=lf
Dockerfile text eol=lf
